fix: 一键启用已适配只启用一部分 —— 目标漏掉「已记已适配但从未通过扫描」的行

根因：routeAdaptUnlockAll 只挑 status==='pending' 的行，而全家桶那批行的记录是
status='adopted' + check='unknown'（账面已适配、从没跑过源码扫描），却仍被补丁禁着
→ targets 为空 → 直接返回「没有待适配行、无需操作」，用户看到的就是只启用了一部分。

改动（B+C）：
- 服务端 routes/plugins.js：目标改为 pending ∪（adopted 且 check !== 'pass'）；
  返回 scanned/unlocked/kept 统计与说明文案
- 客户端 client.js：提示与确认文案改成真实语义 —— 会重跑源码扫描、通过即解锁、
  未通过保持禁用并给出原因（中英各一处）
- 逐行「启用 + 风险确认」的强制通道保留不变

验证：node --check 通过；17/17 套测试全绿；部署副本 38/38 文件哈希一致
